**/__pycache__/
*.pyc
.env
# Secrets — OAuth client + tokens live in $AAKA_CONFIG_DIR/tokens (external),
# never in the repo. Belt-and-suspenders so a stray copy can't be re-added.
**/credentials.json
**/credentials.json.*
**/token*.json
*.pem
*.key
*.db
*.sqlite3
.DS_Store
zeroclaw/config.toml
zeroclaw/workspace/
node_modules/
*.egg-info/
dist/
build/
tmp/
tmp/*.*
samples/demo/data/queue/
samples/demo/logs/
# Regenerated by samples/demo/refresh.py on every webui startup — drifts
# daily. refresh.py + its WEEK/TASKS templates are the source of truth.
samples/demo/data/calendar/
samples/demo/data/tasks/
samples/demo/data/engagement/
venv/

# Website — lives in its own repo (aaka-site)
site/
admin/deploy-site.sh

# Private GTM & strategy docs
docs/gtm/

# Personal/ephemeral working notes
context.md
docs/CONTEXT.md
docs/plan.md
docs/260421_vps_hardening.md
docs/archive/
# Personal pre-commit block patterns (never commit)
.githooks/patterns.local
